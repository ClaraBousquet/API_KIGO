{% extends 'base.html.twig' %}

{% block title %}TypeContact{% endblock %}

{% block body %}
    <h1>TypeContact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_contact.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ type_contact.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_contact_index') }}">back to list</a>

    <a href="{{ path('app_type_contact_edit', {'id': type_contact.id}) }}">edit</a>

    {{ include('type_contact/_delete_form.html.twig') }}
{% endblock %}
